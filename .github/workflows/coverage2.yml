name: Codecov Workflow2

on: [push, pull_request]

jobs:
  build:
    name: "Run specific tests (OS: ${{ matrix.os }} | Py: ${{ matrix.python }})"
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python: ['3.11']

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
          cache: pip

      - name: Setup Chrome
        uses: browser-actions/setup-chrome@v2
        with:
          chrome-version: stable
          install-chromedriver: true

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-cov
          pip install . --upgrade
        shell: bash

      - name: Run selected test scripts and collect coverage
        run: |
          pytest --cov=. --cov-report=xml --cov-report=term-missing tests/test_search_dma.py tests/test_scrape_dma_sb.py
        shell: bash

      - name: Upload coverage reports to Codecov
        if: matrix.os == 'macos-latest' && matrix.python == '3.11'
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          slug: rmj3197/SurVigilance
          files: coverage.xml
          fail_ci_if_error: true
          verbose: true
